interface:
  display_name: "Thoughtbox"
  short_description: "Repo-specific patterns and workflows for thoughtbox"
  default_prompt: "Use the thoughtbox repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true